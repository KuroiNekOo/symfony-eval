# How

- docker compose build --no-cache
- docker compose up -d

<http://localhost:8080/>

### Entrer dans le conteneur

Si vous souhaitez entrer dans le conteneur pour effectuer des commandes avec le CLI de symfony ou composer, etc...

- docker exec -it project2-php-1 bash
